Student Serialization & Deserialization – Java Code
Student.java
import java.io.Serializable;

// Student class implementing Serializable
public class Student implements Serializable {
    private static final long serialVersionUID = 1L;   // Version control for class

    private int studentID;
    private String name;
    private double grade;

    // Constructor
    public Student(int studentID, String name, double grade) {
        this.studentID = studentID;
        this.name = name;
        this.grade = grade;
    }

    // To display student details
    public void displayInfo() {
        System.out.println("Student ID: " + studentID);
        System.out.println("Name      : " + name);
        System.out.println("Grade     : " + grade);
    }
}

SerializeDeserializeDemo.java
import java.io.FileOutputStream;
import java.io.FileInputStream;
import java.io.ObjectOutputStream;
import java.io.ObjectInputStream;

public class SerializeDeserializeDemo {
    public static void main(String[] args) {
        String filename = "studentData.ser";

        // Create a Student object
        Student s1 = new Student(101, "Rahul Sharma", 9.2);

        // SERIALIZATION 
        try (FileOutputStream fos = new FileOutputStream(filename);
             ObjectOutputStream oos = new ObjectOutputStream(fos)) {

            oos.writeObject(s1);   // Serialize object
            System.out.println("Student object serialized successfully.\n");

        } catch (Exception e) {
            System.out.println("Error during serialization: " + e.getMessage());
        }

        //  DESERIALIZATION 
        try (FileInputStream fis = new FileInputStream(filename);
             ObjectInputStream ois = new ObjectInputStream(fis)) {

            Student deserializedStudent = (Student) ois.readObject();  // Deserialize
            System.out.println("Deserialization successful!\n");
            System.out.println("Student Data Retrieved:");
            deserializedStudent.displayInfo();

        } catch (Exception e) {
            System.out.println("Error during deserialization: " + e.getMessage());
        }
    }
}
